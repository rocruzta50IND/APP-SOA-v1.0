# 🏭 PROTOCOL: THE FORGE - STAGE 2B (SWARM: LANDING PAGE)

**hook:** Read and obey `forge/regras.md`.
**Contract:** Read `forge/sandbox/design-system.md` for styling, spacing, and components.
**Context:** Read `forge/forge-context.md` for Brand and Tier identity.

**Role:** You are the Conversion-Focused Landing Page Specialist. Your goal is to build a breathtaking, high-ticket first impression.

## ⚠️ SWARM RESTRICTIONS
1. **ISOLATION:** You are STRICTLY FORBIDDEN from editing `layout.tsx`, `globals.css`, or any file outside your specific scope.
2. **SCOPE:** Build ONLY the root page inside the public route group: `src/app/(public)/page.tsx`.
3. **ZERO GLOBAL POLLUTION:** Do not add new global styles. Use Tailwind classes and the Design System components.

---

### TASK: BUILD THE IMMERSIVE LANDING
- **Route:** Create `src/app/(public)/page.tsx`.
- **Structure:** Fulfill the 5 to 9 sections mandate (Hero, Bento Grid Features, Social Proof/Testimonials, Pricing, FAQ, Footer).
- **Aesthetic:** Follow the Design Tier persona rigorously.
- **Spatial Rhythm:** Use massive vertical whitespace (`py-24` or `py-32`) as defined in the rules to prevent "glue" layouts.
- **Linking:** All call-to-action (CTA) buttons must point to `/login` or `/register`.
- **Dumb UI:** Use mock data only. No state, no logic.

*Print:* > 🎨 **LANDING PAGE (SWARM) CONCLUÍDA!**
> Interface pública gerada com sucesso em `(public)`. O design respira e a conversão está mapeada.
> **[🛑 STOP AND END TASK]**
