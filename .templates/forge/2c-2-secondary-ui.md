# 🏭 PROTOCOL: THE FORGE - STAGE 2C-2 (SECONDARY PAGES & TABLES)

**hook:** Read and obey `forge/regras.md` and `forge/skill-design-tiers.md`. Read `forge/forge-context.md` to know the 5 Internal Routes.

**Role:** You are the Forge Internal UI Architect.
**Environment:** `forge/`

<CRITICAL_UI_RULES>
1. BRAND ICONS: JAMAIS tente importar `Github`, `Google` de `lucide-react`. Use SVGs inline.
2. TYPESCRIPT STRICT MODE: Sempre use asserção em objetos de transição do framer-motion se isolados.
</CRITICAL_UI_RULES>

## ⚠️ STRICT INTERNAL UI DIRECTIVES (PHASE 2C-2)
1. **VISUAL SHELL ONLY:** No APIs. No Backend.
2. **THE SECONDARY MANDATE:** Build the remaining 4 physical internal pages defined in `forge-context.md`.
3. **REALISTIC MOCK DATA:** Any list or data table MUST be populated by a static array with at least 5 realistic mock items related to the niche.

---

### TASK: BUILD SECONDARY PAGES
*Action:* Autonomously execute these steps inside `forge/sandbox/`:

1. **Verify Shell Integration:** Check the previously generated App Shell (`Sidebar.tsx`, `Header.tsx`) to ensure your new pages fit perfectly.
2. **Write the 4 Secondary Pages:**
   - Create the `.tsx` files for the remaining 4 internal routes inside `src/app/`.
   - Populate them with tables, settings forms (purely visual), and grids matching the Design Tier aesthetic rules.
3. *Print:* > 🎨 **FASE 2C-2 CONCLUÍDA!**
> Todas as 5 telas do sistema interno estão prontas e densas.
> **[🛑 STOP AND END TASK]**