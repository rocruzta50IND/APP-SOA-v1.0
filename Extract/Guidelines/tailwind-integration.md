# Tailwind Integration Guide
(Extracted from .claude/skills/design-system/references/tailwind-integration.md)

- Mapping tokens to Tailwind `tailwind.config.ts`.
- Use of CSS variables with HSL format for opacity modifiers (e.g., `bg-primary/50`).
- Implementation of component classes using `@apply`.
- Dark mode configuration using the 'class' strategy.
- Alignment with shadcn/ui conventions.

[Full content available in original source]
